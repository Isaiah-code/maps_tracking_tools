name: maps_tracking_toolbox_example
description: Example app demonstrating Maps Tracking Tools package features
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  maps_tracking_toolbox:
    path: ../

  # Required dependencies
  geolocator: ^12.0.0
  google_maps_flutter: ^2.10.1
  location: ^7.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true